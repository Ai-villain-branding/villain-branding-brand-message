# Railway will auto-detect Node.js 20 from package.json engines field
# We only specify chromium for Playwright
[phases.setup]
nixPkgs = ["chromium"]

[phases.install]
cmds = [
  "npm install",
  "npx playwright install chromium --with-deps"
]

[start]
cmd = "npm start"

